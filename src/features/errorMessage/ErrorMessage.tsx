import { FC, ReactElement } from 'react';

const ErrorMessage: FC = (): ReactElement => {
    return (
        <span>
            Oooops... ошибка...
        </span>
    );
};

export default ErrorMessage;